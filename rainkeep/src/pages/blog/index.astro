---
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog');
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Blog</title>
  </head>
  <body
    style="
      padding: 2rem;
      background: #0e0e0e;
      color: #dcdcdc;
      font-family: 'JetBrains Mono', monospace;
    "
  >
    <main style="max-width: 800px; margin: auto;">
      <h1>Blog</h1>
      <ul>
        {allPosts.map((post) => (
          <li style="margin-bottom: 2rem;">
            <!-- Full debug output -->
            <!-- pre>{JSON.stringify(post, null, 2)}</pre -->

            <!-- Updated: use post.id instead of slug -->
            <!-- p>DEBUG: id = {String(post.id)}</p -->

            <a href={`/blog/${post.id}`} style="color: #79b8ff; font-size: 1.2rem;">
              {post.data.title}
            </a>
            <br />
            <small>{new Date(post.data.pubDate).toDateString()}</small>
            <p>{post.data.description}</p>
          </li>
        ))}
      </ul>
    </main>
  </body>
</html>